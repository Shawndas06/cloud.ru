# TestOps Copilot - Общий обзор системы

## 📋 Содержание
1. [Что такое TestOps Copilot?](#что-такое-testops-copilot)
2. [Архитектура системы](#архитектура-системы)
3. [Микросервисы и их роли](#микросервисы-и-их-роли)
4. [Поток данных](#поток-данных)
5. [Технологический стек](#технологический-стек)
6. [Развертывание](#развертывание)
7. [Полезные ссылки](#полезные-ссылки)

---

## Что такое TestOps Copilot?

**TestOps Copilot** - это AI-powered платформа для автоматической генерации тест-кейсов. Система использует большие языковые модели (LLM) для создания production-ready тестов на основе:
- Веб-страниц (UI тесты)
- OpenAPI спецификаций (API тесты)
- Пользовательских требований

### Основные возможности

1. **Генерация UI тестов** - анализ веб-страниц и создание Playwright тестов
2. **Генерация API тестов** - создание тестов на основе OpenAPI спецификаций
3. **Валидация тестов** - проверка качества и безопасности сгенерированного кода
4. **Оптимизация** - удаление дубликатов и анализ покрытия требований
5. **Интеграции** - экспорт в GitLab, GitHub, Jira, Allure TestOps

---

## Архитектура системы

### Микросервисная архитектура

Система построена на принципах микросервисной архитектуры, где каждый компонент отвечает за свою область:

```
┌─────────────┐
│   Client    │ (Frontend / API клиент)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ API Gateway │ (FastAPI - единая точка входа)
└──────┬──────┘
       │
       ├──────────────┬──────────────┐
       ▼              ▼              ▼
┌──────────┐   ┌──────────┐   ┌──────────┐
│ Workers  │   │  Agents  │   │  Shared  │
│ (Celery) │   │   (AI)   │   │ (Utils)  │
└────┬─────┘   └────┬─────┘   └────┬─────┘
     │              │              │
     └──────────────┴──────────────┘
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
   ┌────────┐  ┌────────┐  ┌────────┐
   │Postgres│  │ Redis  │  │  LLM   │
   │   DB   │  │ Cache  │  │  API   │
   └────────┘  └────────┘  └────────┘
```

### Почему микросервисы?

**Преимущества:**
- **Масштабируемость** - каждый сервис можно масштабировать независимо
- **Независимость** - изменения в одном сервисе не влияют на другие
- **Технологическая гибкость** - можно использовать разные технологии
- **Отказоустойчивость** - падение одного сервиса не останавливает всю систему

**Недостатки:**
- Сложность развертывания
- Необходимость управления сетью между сервисами
- Сложность отладки распределенных систем

**Полезная ссылка:** [Microservices Patterns](https://microservices.io/patterns/index.html)

---

## Микросервисы и их роли

### 1. API Gateway (`api_gateway/`)

**Роль:** Единая точка входа для всех клиентских запросов

**Основные функции:**
- Прием HTTP запросов от клиентов
- Маршрутизация к нужным обработчикам
- Валидация входных данных
- Логирование всех запросов
- Обработка ошибок

**Технологии:** FastAPI, Python

**Подробнее:** [01_API_GATEWAY.md](./01_API_GATEWAY.md)

### 2. Workers (`workers/`)

**Роль:** Асинхронная обработка долгих задач

**Основные функции:**
- Генерация тест-кейсов (может занять минуты)
- Интеграция с GitLab/GitHub
- Обработка фоновых задач

**Технологии:** Celery, Redis (брокер сообщений)

**Подробнее:** [02_WORKERS.md](./02_WORKERS.md)

### 3. Agents (`agents/`)

**Роль:** AI-агенты для различных задач

**Агенты:**
- **ReconnaissanceAgent** - анализ веб-страниц
- **GeneratorAgent** - генерация тестов через LLM
- **ValidatorAgent** - валидация качества тестов
- **OptimizerAgent** - оптимизация и дедупликация
- **TestPlanGeneratorAgent** - генерация тест-планов

**Технологии:** Python, LLM API (Cloud.ru Foundation Models)

**Подробнее:** [03_AGENTS.md](./03_AGENTS.md)

### 4. Shared (`shared/`)

**Роль:** Общие утилиты и компоненты

**Компоненты:**
- **config/** - настройки приложения
- **models/** - модели базы данных (SQLAlchemy)
- **utils/** - утилиты (БД, Redis, LLM клиент, логирование)

**Технологии:** SQLAlchemy, Redis, Pydantic

**Подробнее:** [04_SHARED.md](./04_SHARED.md)

### 5. Frontend (`frontend/`)

**Роль:** Пользовательский интерфейс

**Технологии:** HTML, CSS, JavaScript, Vite

---

## Поток данных

### Пример: Генерация UI тестов

```
1. Клиент → API Gateway
   POST /api/v1/generate/test-cases
   {
     "url": "https://example.com",
     "requirements": ["Проверить вход", "Проверить регистрацию"],
     "test_type": "automated"
   }

2. API Gateway → Database
   Создание записи Request со статусом "pending"

3. API Gateway → Celery Worker
   Запуск задачи generate_test_cases_task.delay()

4. Celery Worker → ReconnaissanceAgent
   Анализ страницы через Playwright
   Результат: структура страницы (кнопки, поля, ссылки)

5. Celery Worker → GeneratorAgent
   Генерация тестов через LLM API
   Входные данные: структура страницы + требования
   Результат: список Python тестов

6. Celery Worker → ValidatorAgent
   Валидация каждого теста
   Проверка: синтаксис, семантика, безопасность

7. Celery Worker → OptimizerAgent (опционально)
   Удаление дубликатов
   Анализ покрытия требований

8. Celery Worker → Database
   Сохранение тестов в таблицу test_cases
   Обновление статуса Request на "completed"

9. Celery Worker → Redis Pub/Sub
   Публикация события о завершении

10. Клиент → API Gateway
    GET /api/v1/tasks/{request_id}
    Получение результатов
```

### Визуализация потока

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. POST /generate
     ▼
┌─────────────┐
│API Gateway │───2.─►┌──────────┐
└────┬────────┘      │ Database │
     │ 3. Task       └──────────┘
     ▼
┌─────────────┐
│   Celery    │
│   Worker    │
└────┬────────┘
     │
     ├─4.─►┌──────────────────┐
     │     │ Reconnaissance   │
     │     │     Agent         │
     │     └────────┬──────────┘
     │              │
     ├─5.─►┌──────────────────┐
     │     │   Generator      │
     │     │     Agent        │──► LLM API
     │     └────────┬──────────┘
     │              │
     ├─6.─►┌──────────────────┐
     │     │   Validator      │
     │     │     Agent        │
     │     └────────┬──────────┘
     │              │
     ├─7.─►┌──────────────────┐
     │     │   Optimizer      │
     │     │     Agent        │
     │     └────────┬──────────┘
     │              │
     ├─8.─►┌──────────┐
     │     │ Database │
     │     └──────────┘
     │
     └─9.─►┌──────────┐
           │  Redis   │
           │  Pub/Sub │
           └──────────┘
```

---

## Технологический стек

### Backend

| Технология | Версия | Назначение |
|------------|--------|------------|
| Python | 3.13 | Основной язык программирования |
| FastAPI | Latest | Веб-фреймворк для API Gateway |
| Celery | Latest | Асинхронная обработка задач |
| SQLAlchemy | Latest | ORM для работы с БД |
| Pydantic | Latest | Валидация данных |
| Playwright | Latest | Автоматизация браузера для анализа страниц |

### Базы данных и кеширование

| Технология | Назначение |
|------------|------------|
| PostgreSQL | Основная база данных |
| pgvector | Расширение для векторных операций |
| Redis | Кеширование и брокер сообщений для Celery |

### AI/ML

| Технология | Назначение |
|------------|------------|
| Cloud.ru Foundation Models API | LLM для генерации тестов |
| OpenAI-compatible API | Интерфейс для работы с LLM |

### Инфраструктура

| Технология | Назначение |
|------------|------------|
| Docker | Контейнеризация |
| Docker Compose | Оркестрация контейнеров |
| Nginx | Веб-сервер для frontend |

### Frontend

| Технология | Назначение |
|------------|------------|
| HTML/CSS/JavaScript | Пользовательский интерфейс |
| Vite | Сборщик frontend |

---

## Развертывание

### Локальное развертывание

1. **Клонирование репозитория**
```bash
git clone <repository_url>
cd testops_copilot
```

2. **Настройка переменных окружения**
```bash
cp .env.example .env
# Отредактировать .env файл
```

3. **Запуск через Docker Compose**
```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL (порт 5432)
- Redis (порт 6379)
- API Gateway (порт 8000)
- Celery Worker
- Flower (мониторинг Celery, порт 5555)
- Frontend (порт 3000)

4. **Проверка работы**
```bash
# API Gateway
curl http://localhost:8000/health

# Документация API
open http://localhost:8000/docs

# Flower (мониторинг задач)
open http://localhost:5555
```

### Структура Docker Compose

```yaml
services:
  postgres:      # База данных
  redis:         # Кеш и брокер
  api_gateway:   # FastAPI приложение
  celery_worker: # Обработчик задач
  flower:        # Мониторинг Celery
  frontend:      # Пользовательский интерфейс
```

**Полезная ссылка:** [Docker Compose Documentation](https://docs.docker.com/compose/)

---

## Полезные ссылки

### Общие концепции

- [Микросервисная архитектура](https://microservices.io/)
- [REST API Best Practices](https://restfulapi.net/)
- [Async Programming в Python](https://docs.python.org/3/library/asyncio.html)

### Технологии

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Celery Documentation](https://docs.celeryq.dev/)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- [Playwright Documentation](https://playwright.dev/python/)

### Базы данных

- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [Redis Documentation](https://redis.io/docs/)
- [pgvector Documentation](https://github.com/pgvector/pgvector)

### Docker

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)

### AI/LLM

- [OpenAI API Documentation](https://platform.openai.com/docs/)
- [Prompt Engineering Guide](https://www.promptingguide.ai/)

---

## Следующие шаги

После изучения общего обзора, рекомендуется изучить каждый компонент подробно:

1. **[API Gateway](./01_API_GATEWAY.md)** - как работает точка входа
2. **[Workers](./02_WORKERS.md)** - асинхронная обработка задач
3. **[Agents](./03_AGENTS.md)** - AI-агенты и их работа
4. **[Shared Components](./04_SHARED.md)** - общие утилиты
5. **[Database Models](./05_DATABASE.md)** - структура базы данных

---

## Часто задаваемые вопросы

### Q: Почему используется Celery вместо простых async функций?

**A:** Celery позволяет:
- Обрабатывать задачи в отдельных процессах (не блокирует API)
- Масштабировать обработку (несколько workers)
- Перезапускать упавшие задачи
- Мониторить выполнение через Flower

Для задач, которые выполняются секунды/минуты, это критично.

### Q: Зачем нужен Redis?

**A:** Redis используется для:
- **Брокер сообщений** - Celery использует Redis для очереди задач
- **Кеширование** - кеш результатов LLM запросов
- **Pub/Sub** - уведомления о статусе задач в реальном времени

### Q: Как работает интеграция с LLM?

**A:** 
1. Получаем токен через IAM API
2. Отправляем промпт в Foundation Models API
3. Получаем сгенерированный код
4. Парсим и валидируем результат

Подробнее в документации по Agents.

---

## Заключение

TestOps Copilot - это комплексная система, объединяющая:
- Современные веб-технологии (FastAPI)
- Асинхронную обработку (Celery)
- AI/ML возможности (LLM)
- Надежное хранение данных (PostgreSQL)
- Быстрый кеш (Redis)

Каждый компонент играет важную роль в общей архитектуре системы. Изучение каждого компонента поможет понять систему в целом.

