# TODO - Что осталось реализовать в проекте

## 📋 Обзор

Данный документ содержит список функций и компонентов, которые еще не реализованы в проекте TestOps Copilot, но упоминаются в ТЗ, настройках или являются необходимыми для полноценной работы системы.

---

## 🔐 1. Аутентификация и Авторизация

### Статус: ❌ Не реализовано

**Текущее состояние:**
- В `settings.py` есть настройки JWT:
  ```python
  jwt_secret_key: str = "your-secret-key-change-in-production"
  jwt_algorithm: str = "HS256"
  jwt_expire_minutes: int = 30
  ```
- Модель `User` в БД есть, но нет API для работы с пользователями
- Нет middleware для проверки JWT токенов
- Нет роутера для регистрации/входа

**Что нужно реализовать:**

1. **Роутер для аутентификации** (`api_gateway/routers/auth.py`):
   - `POST /api/v1/auth/register` - регистрация пользователя
   - `POST /api/v1/auth/login` - вход (получение JWT токена)
   - `POST /api/v1/auth/refresh` - обновление токена
   - `POST /api/v1/auth/logout` - выход
   - `GET /api/v1/auth/me` - информация о текущем пользователе

2. **JWT утилиты** (`shared/utils/jwt.py`):
   - Функции для создания/валидации JWT токенов
   - Декодирование токена и получение user_id

3. **Middleware для проверки токенов** (`api_gateway/middleware/auth_middleware.py`):
   - Проверка JWT токена в заголовке `Authorization: Bearer <token>`
   - Добавление `user` в `request.state` для использования в роутерах

4. **Dependency для защищенных endpoints**:
   ```python
   async def get_current_user(token: str = Depends(oauth2_scheme)):
       # Проверка токена и возврат пользователя
   ```

5. **Хеширование паролей**:
   - Использовать `bcrypt` или `passlib` для хеширования паролей
   - Не хранить пароли в открытом виде

**Полезные ссылки:**
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)
- [JWT в Python](https://pyjwt.readthedocs.io/)

---

## 👥 2. Управление пользователями

### Статус: ❌ Не реализовано

**Текущее состояние:**
- Модель `User` в БД есть со всеми полями
- Нет API endpoints для управления пользователями

**Что нужно реализовать:**

1. **Роутер для пользователей** (`api_gateway/routers/users.py`):
   - `GET /api/v1/users/me` - информация о текущем пользователе
   - `PUT /api/v1/users/me` - обновление профиля
   - `GET /api/v1/users/{user_id}` - информация о пользователе (для админов)
   - `GET /api/v1/users` - список пользователей (для админов)
   - `PUT /api/v1/users/{user_id}` - обновление пользователя (для админов)
   - `DELETE /api/v1/users/{user_id}` - удаление пользователя (для админов)

2. **Роли пользователей**:
   - Добавить поле `role` в модель `User` (admin, user, guest)
   - Middleware для проверки прав доступа

3. **Верификация email**:
   - Отправка письма с подтверждением
   - Обновление поля `is_verified`

---

## 🔑 3. API Keys (Ключи доступа)

### Статус: ⚠️ Частично реализовано

**Текущее состояние:**
- В модели `User` есть поле `api_key`
- В модели `User` есть поле `api_quota_daily`
- Нет генерации API ключей
- Нет использования API ключей для аутентификации

**Что нужно реализовать:**

1. **Генерация API ключей**:
   - `POST /api/v1/users/me/api-keys` - создание нового API ключа
   - `GET /api/v1/users/me/api-keys` - список API ключей пользователя
   - `DELETE /api/v1/users/me/api-keys/{key_id}` - удаление API ключа

2. **Аутентификация через API ключ**:
   - Middleware для проверки API ключа в заголовке `X-API-Key`
   - Альтернатива JWT для программного доступа

3. **Квотирование запросов**:
   - Отслеживание количества запросов в день
   - Проверка `api_quota_daily` перед выполнением запроса
   - Возврат ошибки 429 при превышении лимита

---

## 🚦 4. Rate Limiting (Ограничение частоты запросов)

### Статус: ❌ Не реализовано

**Текущее состояние:**
- В `settings.py` есть настройки:
  ```python
  rate_limit_per_minute: int = 100
  rate_limit_burst: int = 10
  ```
- Нет middleware для rate limiting

**Что нужно реализовать:**

1. **Rate Limiting Middleware** (`api_gateway/middleware/rate_limit_middleware.py`):
   - Использовать Redis для хранения счетчиков
   - Token bucket или sliding window алгоритм
   - Разные лимиты для разных endpoints
   - Лимиты на основе user_id или IP адреса

2. **Интеграция с FastAPI**:
   ```python
   from slowapi import Limiter, _rate_limit_exceeded_handler
   from slowapi.util import get_remote_address
   
   limiter = Limiter(key_func=get_remote_address)
   app.state.limiter = limiter
   ```

3. **Ответ при превышении лимита**:
   - HTTP 429 Too Many Requests
   - Заголовки `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`

**Полезные ссылки:**
- [slowapi - Rate Limiting для FastAPI](https://github.com/laurents/slowapi)

---

## 🔄 5. Автоматическое обновление токена Evolution Compute

### Статус: ⚠️ Частично реализовано

**Текущее состояние:**
- Есть скрипт `auto_refresh_token.py` в папке с ТЗ
- Скрипт обновляет токен в `.env` файле
- Нет интеграции в основное приложение
- Нет автоматического запуска при старте приложения

**Что нужно реализовать:**

1. **Интеграция в LLM Client**:
   - Автоматическое обновление токена при истечении
   - Использование `EVOLUTION_COMPUTE_TOKEN` из переменных окружения
   - Обновление токена в памяти без перезапуска

2. **Фоновый процесс**:
   - Запуск в отдельном потоке при старте приложения
   - Обновление токена каждые 50 минут (до истечения 1 часа)

3. **Альтернатива:**
   - Использовать существующий скрипт через subprocess
   - Или интегрировать логику напрямую в приложение

---

## 🧪 6. LangGraph Workflow

### Статус: ⚠️ Заглушка

**Текущее состояние:**
- Файл `workers/tasks/langgraph_workflow.py` существует
- Только заглушка, нет реальной реализации
- В модели `Request` есть поле `langgraph_thread_id`, но не используется

**Что нужно реализовать:**

1. **Установка зависимостей**:
   ```bash
   pip install langgraph langchain
   ```

2. **Реализация workflow**:
   - Определение состояний (State)
   - Определение узлов (Nodes)
   - Определение графа переходов
   - Checkpoint для сохранения состояния

3. **Интеграция с Celery**:
   - Использование LangGraph для сложных сценариев
   - Сохранение `thread_id` в БД
   - Восстановление состояния при перезапуске

**Полезные ссылки:**
- [LangGraph Documentation](https://langchain-ai.github.io/langgraph/)

---

## 🛡️ 7. Sandbox для SafetyGuard

### Статус: ❌ Не реализовано

**Текущее состояние:**
- В `SafetyGuard` есть комментарий:
  ```python
  # Level 4: Sandbox (опционально, не реализовано в MVP)
  # if settings.safety_guard_sandbox_enabled:
  #     level4_result = self._sandbox_execution(test_code)
  ```
- В `settings.py` есть настройка `safety_guard_sandbox_enabled: bool = False`

**Что нужно реализовать:**

1. **Sandbox окружение**:
   - Изолированное выполнение кода (Docker контейнер)
   - Ограничение ресурсов (CPU, память, время)
   - Ограничение доступа к файловой системе и сети

2. **Метод `_sandbox_execution`**:
   - Запуск теста в изолированном окружении
   - Мониторинг выполнения
   - Анализ поведения (файловые операции, сетевые запросы)

3. **Интеграция с SafetyGuard**:
   - Включение sandbox проверки при `safety_guard_sandbox_enabled=True`
   - Блокировка опасного кода на основе результатов sandbox

**Альтернативы:**
- Использовать `subprocess` с ограничениями
- Использовать Docker API для создания временных контейнеров
- Использовать специализированные библиотеки (например, `pysandbox`)

---

## 🗄️ 8. pgvector для Semantic Embeddings

### Статус: ⚠️ Частично реализовано

**Текущее состояние:**
- В модели `TestCase` есть поле `semantic_embedding: Column(Text, nullable=True)`
- Комментарий: `# VECTOR(768) - будет через pgvector`
- В `docker-compose.yml` используется `pgvector/pgvector:pg15`
- В `LLMClient.generate_embeddings()` используется hash-based подход (не настоящие embeddings)

**Что нужно реализовать:**

1. **Установка расширения pgvector**:
   - В `scripts/init_db.sql` добавить: `CREATE EXTENSION IF NOT EXISTS vector;`

2. **Обновление модели TestCase**:
   ```python
   from pgvector.sqlalchemy import Vector
   
   semantic_embedding = Column(Vector(384), nullable=True)  # или 768
   ```

3. **Генерация настоящих embeddings**:
   - Использовать LLM API для генерации embeddings (если поддерживается)
   - Или использовать специализированные модели (sentence-transformers)

4. **Поиск похожих тестов**:
   - Использовать векторный поиск PostgreSQL
   - Запрос: `SELECT * FROM test_cases ORDER BY semantic_embedding <-> $1 LIMIT 10`

**Полезные ссылки:**
- [pgvector Documentation](https://github.com/pgvector/pgvector)
- [pgvector Python](https://github.com/pgvector/pgvector-python)

---

## 📊 9. Метрики и Мониторинг

### Статус: ⚠️ Частично реализовано

**Текущее состояние:**
- Есть роутер `/metrics` для Prometheus
- Есть базовые метрики (HTTP requests, LLM requests)
- Нет сохранения метрик в БД
- Нет дашбордов и алертов

**Что нужно реализовать:**

1. **Сохранение метрик в БД**:
   - Использовать модель `GenerationMetric` (уже есть)
   - Сохранять метрики каждого агента
   - Агрегация метрик по времени

2. **Дашборды**:
   - Grafana для визуализации
   - Дашборды для:
     - Количество сгенерированных тестов
     - Время выполнения задач
     - Использование LLM (токены, стоимость)
     - Ошибки и их типы

3. **Алерты**:
   - Настройка алертов в Prometheus/Grafana
   - Уведомления при:
     - Высоком проценте ошибок
     - Превышении времени выполнения
     - Превышении лимита токенов/стоимости

4. **Health checks**:
   - Расширить `/health` endpoint
   - Проверка доступности БД, Redis, LLM API
   - Проверка доступности внешних сервисов (GitHub, GitLab, Jira)

---

## 🧪 10. Тестирование

### Статус: ❌ Не реализовано

**Текущее состояние:**
- Нет unit тестов
- Нет integration тестов
- Нет тестов для API endpoints

**Что нужно реализовать:**

1. **Unit тесты**:
   - Тесты для агентов (GeneratorAgent, ValidatorAgent, etc.)
   - Тесты для утилит (database, redis, llm_client)
   - Использовать `pytest` и `pytest-asyncio`

2. **Integration тесты**:
   - Тесты для API endpoints
   - Тесты для Celery задач
   - Использовать `TestClient` из FastAPI

3. **Тесты для БД**:
   - Использовать тестовую БД
   - Фикстуры для создания/удаления данных

4. **CI/CD**:
   - GitHub Actions или GitLab CI
   - Автоматический запуск тестов при коммитах
   - Проверка покрытия кода

**Структура:**
```
tests/
├── unit/
│   ├── test_agents/
│   ├── test_utils/
│   └── test_models/
├── integration/
│   ├── test_api/
│   └── test_workers/
└── fixtures/
```

---

## 📝 11. Документация API

### Статус: ⚠️ Частично реализовано

**Текущее состояние:**
- FastAPI автоматически генерирует Swagger UI на `/docs`
- Есть описания endpoints
- Нет примеров запросов/ответов
- Нет описания ошибок

**Что нужно улучшить:**

1. **Примеры запросов/ответов**:
   - Добавить `examples` в Pydantic модели
   - Добавить примеры в docstrings

2. **Описание ошибок**:
   - Документировать все возможные HTTP статусы
   - Описать формат ошибок

3. **Postman коллекция**:
   - Экспорт API в Postman
   - Примеры для всех endpoints

---

## 🔍 12. Поиск и Фильтрация

### Статус: ❌ Не реализовано

**Что нужно реализовать:**

1. **Поиск тестов**:
   - `GET /api/v1/tests?search=<query>` - поиск по названию/коду
   - Полнотекстовый поиск в PostgreSQL

2. **Фильтрация**:
   - `GET /api/v1/tests?status=passed&type=automated`
   - Фильтры по дате, статусу, типу, приоритету

3. **Сортировка**:
   - `GET /api/v1/tests?sort=created_at&order=desc`

4. **Пагинация**:
   - `GET /api/v1/tests?page=1&per_page=20`

---

## 📤 13. Экспорт тестов

### Статус: ⚠️ Частично реализовано

**Текущее состояние:**
- Есть экспорт в GitHub/GitLab
- Нет экспорта в другие форматы

**Что нужно реализовать:**

1. **Экспорт в файлы**:
   - `GET /api/v1/tests/{request_id}/export?format=zip`
   - Форматы: ZIP, TAR, отдельные файлы

2. **Экспорт в Allure TestOps**:
   - Прямая интеграция с Allure TestOps API
   - Создание тест-кейсов в Allure

3. **Экспорт в другие форматы**:
   - JSON
   - YAML
   - CSV (для отчетов)

---

## 🔔 14. Уведомления

### Статус: ⚠️ Частично реализовано

**Текущее состояние:**
- Есть Redis Pub/Sub для real-time обновлений
- Нет email уведомлений
- Нет webhook уведомлений

**Что нужно реализовать:**

1. **Email уведомления**:
   - Уведомление о завершении генерации
   - Уведомление об ошибках
   - Использовать SMTP или сервисы (SendGrid, Mailgun)

2. **Webhook уведомления**:
   - Настройка webhook URL в настройках пользователя
   - Отправка POST запросов при событиях

3. **Интеграция с системами**:
   - Slack уведомления
   - Telegram бот
   - Microsoft Teams

---

## 🎨 15. Frontend улучшения

### Статус: ⚠️ Базовый функционал

**Текущее состояние:**
- Есть базовый frontend
- Нет полноценного UI/UX

**Что нужно улучшить:**

1. **Улучшение UI**:
   - Современный дизайн
   - Адаптивность (mobile-friendly)
   - Темная тема

2. **Функционал**:
   - Просмотр истории запросов
   - Управление тестами
   - Настройки пользователя
   - Дашборды и графики

3. **UX улучшения**:
   - Загрузка файлов drag-and-drop
   - Real-time обновления через SSE
   - Прогресс-бары для долгих операций

---

## 📋 Приоритеты реализации

### Высокий приоритет (MVP):
1. ✅ Аутентификация и авторизация (JWT)
2. ✅ Rate Limiting
3. ✅ Управление пользователями
4. ✅ API Keys

### Средний приоритет:
5. ✅ Автоматическое обновление токена Evolution Compute
6. ✅ Тестирование (unit, integration)
7. ✅ Поиск и фильтрация
8. ✅ Улучшение документации API

### Низкий приоритет (будущие улучшения):
9. ✅ LangGraph Workflow
10. ✅ Sandbox для SafetyGuard
11. ✅ pgvector для Semantic Embeddings
12. ✅ Расширенный мониторинг и алерты
13. ✅ Уведомления (email, webhook)
14. ✅ Улучшение Frontend

---

## 📝 Заметки

- Некоторые функции могут быть опциональными в зависимости от требований
- Приоритеты могут меняться в зависимости от бизнес-требований
- Рекомендуется реализовывать функции постепенно, начиная с высокого приоритета

---

**Последнее обновление:** 2024

