# Workers (Celery) - Подробное объяснение для Junior разработчиков

## 📋 Содержание
1. [Что такое Celery и зачем он нужен?](#что-такое-celery-и-зачем-он-нужен)
2. [Архитектура Celery](#архитектура-celery)
3. [Структура Workers в проекте](#структура-workers-в-проекте)
4. [Создание и настройка Celery приложения](#создание-и-настройка-celery-приложения)
5. [Создание задач (Tasks)](#создание-задач-tasks)
6. [Workflow генерации тестов](#workflow-генерации-тестов)
7. [Обработка ошибок](#обработка-ошибок)
8. [Мониторинг задач](#мониторинг-задач)
9. [Полезные ссылки](#полезные-ссылки)

---

## Что такое Celery и зачем он нужен?

### Проблема: Долгие операции

Представьте ситуацию:
- Пользователь отправляет запрос на генерацию тестов
- Генерация занимает 2-3 минуты
- Если делать это синхронно, пользователь ждет 2-3 минуты с открытым соединением
- Если 10 пользователей одновременно - сервер не справляется

### Решение: Асинхронная обработка

**Celery** - это система распределенной обработки задач (distributed task queue).

**Как это работает:**
1. API Gateway получает запрос
2. Создает задачу и отправляет в очередь (Redis)
3. Сразу возвращает ответ: "Задача принята, ID: 12345"
4. Celery Worker (отдельный процесс) забирает задачу из очереди
5. Выполняет долгую операцию
6. Сохраняет результат в БД
7. Клиент может проверить статус через API

**Аналогия:** Как ресторан - официант принимает заказ (быстро), передает на кухню, а повар готовит (долго).

### Преимущества Celery

1. **Не блокирует API** - API Gateway быстро отвечает
2. **Масштабируемость** - можно запустить несколько workers
3. **Надежность** - задачи не теряются при перезапуске
4. **Мониторинг** - видно статус всех задач
5. **Retry** - автоматический повтор при ошибках

**Полезная ссылка:** [Celery Documentation](https://docs.celeryq.dev/)

---

## Архитектура Celery

### Компоненты

```
┌─────────────┐
│   Client    │ (API Gateway)
└──────┬──────┘
       │ 1. Запуск задачи
       │    task.delay(...)
       ▼
┌─────────────┐
│   Broker    │ (Redis - очередь задач)
│   (Queue)   │
└──────┬──────┘
       │ 2. Worker забирает задачу
       ▼
┌─────────────┐
│   Worker    │ (Отдельный процесс)
│  (Celery)   │
└──────┬──────┘
       │ 3. Выполнение задачи
       │    (генерация тестов)
       ▼
┌─────────────┐
│   Backend   │ (Redis - результаты)
│  (Results)  │
└─────────────┘
```

### Терминология

- **Broker** - очередь сообщений (Redis, RabbitMQ)
- **Worker** - процесс, который выполняет задачи
- **Task** - функция, которая выполняется асинхронно
- **Result Backend** - хранилище результатов (Redis, PostgreSQL)

---

## Структура Workers в проекте

### Структура файлов

```
workers/
├── celery_app.py              # Настройка Celery приложения
└── tasks/
    ├── generate_workflow.py    # Генерация UI тестов
    ├── generate_api_workflow.py # Генерация API тестов
    ├── langgraph_workflow.py   # LangGraph workflow
    ├── github_integration.py   # Интеграция с GitHub
    └── gitlab_integration.py   # Интеграция с GitLab
```

### Основные задачи

1. **generate_test_cases_task** - генерация UI тестов
2. **generate_api_tests_task** - генерация API тестов
3. **github_integration_task** - экспорт в GitHub
4. **gitlab_integration_task** - экспорт в GitLab

---

## Создание и настройка Celery приложения

### Файл: `workers/celery_app.py`

```python
from celery import Celery
from shared.config.settings import settings

# Создание Celery приложения
celery_app = Celery(
    "testops_copilot",                    # Имя приложения
    broker=settings.celery_broker,       # URL брокера (Redis)
    backend=settings.celery_result,      # URL для результатов
    include=[                            # Модули с задачами
        "workers.tasks.generate_workflow",
        "workers.tasks.generate_api_workflow",
    ]
)
```

**Разбор по частям:**

1. **`Celery(...)`** - создание приложения
   - `"testops_copilot"` - имя приложения (для идентификации)
   - `broker` - где хранить очередь задач (Redis URL)
   - `backend` - где хранить результаты (Redis URL)
   - `include` - какие модули загружать (где искать задачи)

2. **Конфигурация:**

```python
celery_app.conf.update(
    task_serializer="json",              # Формат сериализации
    accept_content=["json"],             # Принимать только JSON
    result_serializer="json",            # Формат результатов
    timezone="UTC",                      # Часовой пояс
    enable_utc=True,                    # Использовать UTC
    task_track_started=True,             # Отслеживать начало задач
    task_time_limit=3600,                # Максимальное время (1 час)
    task_soft_time_limit=3540,           # Мягкий лимит (59 минут)
    worker_prefetch_multiplier=1,        # Сколько задач брать сразу
    worker_max_tasks_per_child=50,       # Перезапуск worker после N задач
)
```

**Объяснение параметров:**

- **`task_serializer="json"`** - задачи передаются в формате JSON (читаемо, отлаживаемо)
- **`task_time_limit=3600`** - задача не может выполняться больше часа (защита от зависаний)
- **`task_soft_time_limit=3540`** - за 1 минуту до жесткого лимита задача получит исключение (можно корректно завершить)
- **`worker_prefetch_multiplier=1`** - worker берет по 1 задаче (для долгих задач)
- **`worker_max_tasks_per_child=50`** - после 50 задач worker перезапускается (защита от утечек памяти)

**Полезная ссылка:** [Celery Configuration](https://docs.celeryq.dev/en/stable/userguide/configuration.html)

---

## Создание задач (Tasks)

### Базовый пример

```python
from workers.celery_app import celery_app

@celery_app.task
def simple_task(x, y):
    """Простая задача - сложение чисел"""
    return x + y
```

**Использование:**

```python
# Запуск задачи асинхронно
result = simple_task.delay(2, 3)

# Получение результата (блокирующий вызов)
print(result.get())  # 5
```

### Декоратор `@celery_app.task`

**Что делает:**
- Регистрирует функцию как задачу Celery
- Позволяет вызывать через `.delay()` или `.apply_async()`
- Автоматически сериализует аргументы
- Отслеживает выполнение

### Продвинутый пример: Генерация тестов

```python
@celery_app.task(
    bind=True,                          # Передавать self в функцию
    base=GenerateWorkflowTask,          # Базовый класс с обработкой ошибок
    name="workers.tasks.generate_workflow.generate_test_cases_task"
)
def generate_test_cases_task(
    self,                               # self доступен благодаря bind=True
    request_id: str,
    url: str,
    requirements: list,
    test_type: str,
    options: dict = None
):
    """Генерация UI тест-кейсов"""
    # Логика генерации...
    return {"status": "completed"}
```

**Разбор:**

1. **`bind=True`** - передает объект задачи (`self`) в функцию
   - Позволяет обновлять статус: `self.update_state(state='PROGRESS', meta={'step': 'generation'})`
   - Доступ к `self.request.id` (ID задачи)

2. **`base=GenerateWorkflowTask`** - базовый класс
   - Обработка ошибок через `on_failure()`
   - Обновление статуса в БД при ошибке

3. **`name="..."`** - явное имя задачи
   - По умолчанию: `module.function_name`
   - Явное имя упрощает отладку

**Полезная ссылка:** [Celery Tasks](https://docs.celeryq.dev/en/stable/userguide/tasks.html)

---

## Workflow генерации тестов

### Полный процесс: `generate_test_cases_task`

Давайте разберем пошагово, что происходит при генерации тестов:

#### Шаг 1: Обновление статуса в БД

```python
with get_db() as db:
    request = db.query(Request).filter(Request.request_id == uuid.UUID(request_id)).first()
    if not request:
        raise ValueError(f"Request {request_id} not found")
    
    request.status = "processing"
    request.started_at = datetime.utcnow()
    db.commit()
```

**Что происходит:**
- Находим запрос в БД по `request_id`
- Меняем статус с "pending" на "processing"
- Сохраняем время начала

**Зачем:** Клиент может проверить статус через API.

#### Шаг 2: Публикация события начала

```python
redis_client.publish_event(
    f"request:{request_id}",
    {"status": "processing", "step": "reconnaissance"}
)
```

**Что происходит:**
- Публикуем событие в Redis Pub/Sub
- Клиент может подписаться и получать обновления в реальном времени

**Полезная ссылка:** [Redis Pub/Sub](https://redis.io/docs/manual/pubsub/)

#### Шаг 3: Reconnaissance - анализ страницы

```python
recon_agent = ReconnaissanceAgent()
page_structure = recon_agent.analyze_page(url, timeout=60)
```

**Что происходит:**
- Запускается Playwright (браузер)
- Открывается страница по URL
- Извлекаются элементы: кнопки, поля ввода, ссылки
- Возвращается структура страницы

**Результат:**
```python
{
    "title": "Login Page",
    "url": "https://example.com/login",
    "buttons": [
        {"text": "Login", "selector": "[data-testid='login-btn']", "visible": True},
        ...
    ],
    "inputs": [
        {"name": "username", "type": "text", "selector": "#username", "visible": True},
        ...
    ],
    "links": [...]
}
```

**Подробнее:** См. документацию по Agents.

#### Шаг 4: Generation - генерация тестов

```python
generator = GeneratorAgent()

# Запуск асинхронной генерации
loop = asyncio.new_event_loop()
asyncio.set_event_loop(loop)
try:
    tests = loop.run_until_complete(
        generator.generate_ui_tests(
            url=url,
            page_structure=page_structure,
            requirements=requirements,
            test_type=test_type,
            options=options
        )
    )
finally:
    loop.close()
```

**Что происходит:**
- Создается промпт для LLM на основе структуры страницы
- Отправляется запрос в LLM API
- Получается сгенерированный Python код
- Парсится код на отдельные тесты

**Почему `asyncio.new_event_loop()`?**
- Celery задачи выполняются синхронно
- Но `GeneratorAgent.generate_ui_tests()` - асинхронная функция
- Нужно создать event loop для выполнения async функции

**Альтернатива:** Можно использовать `asyncio.run()`, но это создает новый loop каждый раз.

**Полезная ссылка:** [Python asyncio](https://docs.python.org/3/library/asyncio.html)

#### Шаг 5: Validation - валидация тестов

```python
validator = ValidatorAgent()
validated_tests = []

for test_code in tests:
    validation_result = validator.validate(test_code, validation_level="full")
    
    if validation_result.get("passed", False):
        validated_tests.append({
            "code": test_code,
            "validation": validation_result
        })
    else:
        print(f"Validation failed: {validation_result.get('errors', [])}")
```

**Что происходит:**
- Каждый тест проверяется на:
  - Синтаксис (AST парсинг)
  - Семантику (наличие Allure декораторов)
  - Логику (нет бесконечных циклов)
  - Безопасность (Safety Guard)
- Только валидные тесты сохраняются

**Подробнее:** См. документацию по ValidatorAgent.

#### Шаг 6: Optimization - оптимизация (опционально)

```python
if options.get("optimize", True) and len(validated_tests) > 1:
    optimizer = OptimizerAgent()
    optimization_result = await optimizer.optimize(
        tests=[{"test_id": str(uuid.uuid4()), "test_code": t["code"]} for t in validated_tests],
        requirements=requirements,
        options=options
    )
    optimized_tests = optimization_result.get("optimized_tests", [])
```

**Что происходит:**
- Находятся дубликаты (точные и семантические)
- Анализируется покрытие требований
- Удаляются дубликаты
- Возвращается оптимизированный список

**Подробнее:** См. документацию по OptimizerAgent.

#### Шаг 7: Сохранение в БД

```python
with get_db() as db:
    request = db.query(Request).filter(Request.request_id == uuid.UUID(request_id)).first()
    
    saved_tests = []
    for test_data in optimized_tests:
        test_code = test_data["code"]
        code_hash = hashlib.sha256(test_code.encode()).hexdigest()
        
        test_case = TestCase(
            request_id=request.request_id,
            test_name=test_name,
            test_code=test_code,
            test_type=actual_test_type,
            code_hash=code_hash,
            validation_status="passed",
            validation_issues=test_data.get("validation", {}).get("errors", [])
        )
        db.add(test_case)
        saved_tests.append({"test_id": str(test_case.test_id), "test_name": test_name})
    
    request.status = "completed"
    request.completed_at = datetime.utcnow()
    request.result_summary = {
        "tests_generated": len(saved_tests),
        "tests_validated": len(validated_tests),
        "tests_optimized": len(optimized_tests),
    }
    db.commit()
```

**Что происходит:**
- Для каждого теста создается запись в таблице `test_cases`
- Вычисляется хеш кода (для дедупликации)
- Обновляется статус запроса на "completed"
- Сохраняется summary с метриками

#### Шаг 8: Публикация события завершения

```python
redis_client.publish_event(
    f"request:{request_id}",
    {
        "status": "completed",
        "tests_count": len(saved_tests),
        "result_summary": request.result_summary
    }
)
```

**Что происходит:**
- Клиент, подписанный на канал `request:{request_id}`, получает уведомление
- Может обновить UI без polling

---

## Обработка ошибок

### Базовый класс для обработки ошибок

```python
class GenerateWorkflowTask(Task):
    """Базовый класс для задачи генерации с обработкой ошибок"""
    
    def on_failure(self, exc, task_id, args, kwargs, einfo):
        """Вызывается при ошибке задачи"""
        request_id = kwargs.get("request_id") or (args[0] if args else None)
        if request_id:
            with get_db() as db:
                request = db.query(Request).filter(Request.request_id == uuid.UUID(request_id)).first()
                if request:
                    request.status = "failed"
                    request.error_message = str(exc)
                    db.commit()
```

**Что происходит:**
- При любой ошибке в задаче вызывается `on_failure()`
- Обновляется статус запроса в БД на "failed"
- Сохраняется сообщение об ошибке
- Клиент может увидеть причину ошибки

### Try-Except в задаче

```python
def generate_test_cases_task(self, ...):
    try:
        # Основная логика
        ...
        return {"status": "completed"}
    
    except Exception as e:
        # Логирование
        error_msg = str(e)
        print(f"Error: {error_msg}")
        import traceback
        traceback.print_exc()
        
        # Обновление статуса в БД
        try:
            with get_db() as db:
                request = db.query(Request).filter(Request.request_id == uuid.UUID(request_id)).first()
                if request:
                    request.status = "failed"
                    request.error_message = error_msg
                    db.commit()
        except Exception as db_error:
            print(f"Error updating request status: {db_error}")
        
        # Публикация события об ошибке
        try:
            redis_client.publish_event(
                f"request:{request_id}",
                {"status": "failed", "error": error_msg}
            )
        except Exception:
            pass
        
        raise  # Пробрасываем ошибку дальше
```

**Зачем два уровня обработки?**
- `on_failure()` - гарантированно вызывается (даже если исключение не поймано)
- `try-except` - позволяет более точно обработать ошибку

---

## Мониторинг задач

### Flower - веб-интерфейс для мониторинга

**Запуск:**
```bash
celery -A workers.celery_app flower --port=5555
```

**Доступ:** http://localhost:5555

**Что можно увидеть:**
- Список всех задач (pending, started, success, failure)
- Детали задачи (аргументы, результат, время выполнения)
- Статистика workers (активные, обработанные задачи)
- Графики производительности

**Полезная ссылка:** [Flower Documentation](https://flower.readthedocs.io/)

### Проверка статуса через API

```python
# В API Gateway
@router.get("/tasks/{task_id}")
async def get_task_status(task_id: str):
    task = celery_app.AsyncResult(task_id)
    
    return {
        "task_id": task_id,
        "status": task.status,  # PENDING, STARTED, SUCCESS, FAILURE
        "result": task.result if task.ready() else None,
        "info": task.info
    }
```

**Статусы задач:**
- **PENDING** - задача в очереди
- **STARTED** - задача выполняется
- **SUCCESS** - задача выполнена успешно
- **FAILURE** - задача завершилась с ошибкой
- **RETRY** - задача повторяется

---

## Полезные ссылки

### Celery

- [Celery Documentation](https://docs.celeryq.dev/)
- [Celery Best Practices](https://docs.celeryq.dev/en/stable/userguide/tasks.html#best-practices)
- [Celery Configuration](https://docs.celeryq.dev/en/stable/userguide/configuration.html)

### Redis

- [Redis Documentation](https://redis.io/docs/)
- [Redis Pub/Sub](https://redis.io/docs/manual/pubsub/)

### Python Async

- [Python asyncio](https://docs.python.org/3/library/asyncio.html)
- [AsyncIO Tutorial](https://realpython.com/async-io-python/)

### Мониторинг

- [Flower Documentation](https://flower.readthedocs.io/)

---

## Часто задаваемые вопросы

### Q: Почему не использовать FastAPI BackgroundTasks?

**A:** FastAPI BackgroundTasks выполняются в том же процессе, что и API. Если задача долгая:
- Блокирует обработку других запросов
- При перезапуске API задачи теряются
- Нет мониторинга и retry

Celery решает все эти проблемы.

### Q: Как масштабировать обработку?

**A:** Запустить несколько workers:
```bash
# Запуск 4 workers
celery -A workers.celery_app worker --concurrency=4

# Или в Docker Compose добавить еще один сервис
```

### Q: Что делать, если задача зависла?

**A:** Celery автоматически убьет задачу после `task_time_limit`. Можно также:
- Отменить задачу: `task.revoke(terminate=True)`
- Проверить в Flower и отменить вручную

### Q: Как отладить задачу локально?

**A:** 
1. Запустить worker в режиме отладки: `celery -A workers.celery_app worker --loglevel=debug`
2. Вызвать задачу синхронно: `generate_test_cases_task.apply(args=[...])` (вместо `.delay()`)
3. Использовать print/logging для отладки

---

## Заключение

Workers (Celery) - это критически важный компонент системы, который:
- Обрабатывает долгие операции асинхронно
- Не блокирует API Gateway
- Масштабируется горизонтально
- Обеспечивает надежность и мониторинг

Понимание работы Celery необходимо для понимания всей системы!

